<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BumbleFormula.MainPage">
    <ScrollView>
        <VerticalStackLayout>
            <Label Text="Welcome to Bumble Formular!" 
                   VerticalOptions="Center" 
                   HorizontalOptions="Center" 
                   FontSize="Large"/>
            <Button x:Name="StartButton" Text="Start Classification" Clicked="OnStartClicked"/>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
